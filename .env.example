# Copy to .env for local testing (`pip install python-dotenv` and load it yourself,
# or just `export $(cat .env | xargs)` before running). In GitHub Actions these are
# read from repository secrets instead — see README.md.

ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-5

GMAIL_ADDRESS=you@gmail.com
GMAIL_APP_PASSWORD=your16charapppassword
RECIPIENT_EMAIL=you@gmail.com

ENERGYNEWS_USERNAME=braam conradie
ENERGYNEWS_PASSWORD=energy

BRIEFING_TIMEZONE=Pacific/Auckland
